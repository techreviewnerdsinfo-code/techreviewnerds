export function buildItemList(items: { url: string; name: string }[]) {
  return JSON.stringify({ /* schema contents… */ });
}

export function buildProductOffer(args) {
  // …
}

export function buildFAQPage(faqs) {
  // …
}
